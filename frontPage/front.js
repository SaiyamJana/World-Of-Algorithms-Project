function goTo(page) {
    // In the future, you can link this to actual pages
    alert(`This would take you to: ${page}`);
    // window.location.href = page; // Uncomment when you have the pages ready
}
